#include <iostream>

using namespace std;

int main()
{
    int n, a, par = -1;
   
    cin >> n;
    
    while(n){
        cin >> a;
        if (a % 2 == 0){
            par = a;
        }
        n --;
    }
    
    if(par == -1)
        cout << "IMPOSIBIL";
    else
        cout << par;
}
